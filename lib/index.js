"use strict";

/**
 * ytPlayer
 * A friendly wrapper around the YouTube Iframe API.
 *
 * @name ytPlayer
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function ytPlayer (a, b) {
    return a + b;
};